{# placeholder for template management #}

{% if item.singleton is defined and item.singleton %}
data "template_file" "{{item.name}}" {
  template = <<EOF
{% include item.source %}

EOF
  vars {
    vpc = "${local.vpc}"

{% for var in (item.vars|default({})).keys() %}
    {{var}} = "{{item.vars[var]}}"
{% endfor %}
  }
}
{% endif %}
